---
apiVersion: v1
kind: Secret
metadata:
  name: n8n-secrets
  namespace: mydomain-dev
type: Opaque
data:
  # IMPORTANT: Replace with your own generated encryption key (base64 encoded)
  # Generate a secure key with: head -c 32 /dev/urandom | base64 -w 0
  # Example: echo -n "your-secure-encryption-key-at-least-32-chars" | base64 -w 0
  # Store the original key securely and never commit it to version control!
  encryption-key: "UkVQTEFDRV9XSVRIX1lPVVJfQkFTRTY0X0VOQ09ERURfS0VZ"  # This is "REPLACE_WITH_YOUR_BASE64_ENCODED_KEY" encoded
