apiVersion: v1
kind: Secret
metadata:
  name: n8n-secrets
  namespace: mydomain-dev
type: Opaque
data:
  # Replace with your own generated key (base64 encoded)
  # Example: echo -n "your-secure-encryption-key-at-least-32-chars" | base64
  encryption-key: <kept in keepass>
